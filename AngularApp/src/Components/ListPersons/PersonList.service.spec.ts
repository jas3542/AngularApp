import { ListPersonsComponent } from './ListPersons.component';
import { inject } from '@angular/core/testing';

describe('Component: ListPersons', () =>
{
  beforeEach(() => {
   
  });

  afterEach(() => {

  });

  beforeAll(() => {

  });

  afterAll(() => {
    
  });
});
